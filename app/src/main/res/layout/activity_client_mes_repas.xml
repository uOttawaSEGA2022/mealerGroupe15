<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".clientclasses.ClientMesRepasActivity"
    >



    <LinearLayout
        android:layout_width="match_parent"

        android:layout_height="match_parent"
        android:background="@drawable/backgroundk"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/textView400"
            style="@style/Theme.AppCompat.DayNight"
            android:layout_width="match_parent"
            android:layout_height="90dp"
            android:layout_marginTop="35dp"
            android:text="         Mes Repas"
            android:textSize="40dp"
            android:textStyle="bold" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/commandeRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="539dp"
            android:background="@drawable/logodemealerremove" />

        <Button
            android:id="@+id/BacktoMainActivityClient"
            android:layout_width="123dp"
            android:layout_height="wrap_content"
            android:onClick="OnBackToMainActivityClient"
            android:background="@color/purple_200"
            android:text="Back"
            app:cornerRadius="40dp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>